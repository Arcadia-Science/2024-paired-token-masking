"""A subpackage for all the Modal code.

We use Modal to calculate all the ESM embeddings used in this analysis.

Note that the outputs of the Modal-dependent code are included in the repo, so this
dependence on Modal doesn't prevent anyone from running the notebook themselves.

Resources:
    - https://modal.com/
"""
